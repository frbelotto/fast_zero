{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Python: Run app.py as module",
            "type": "debugpy",
            "request": "launch",
            "module": "fast_zero.app",
            "cwd": "${workspaceFolder}",
            "console": "integratedTerminal"
        }
    ]
}
